global:
  scrape_interval: 15s

scrape_configs:
  # booking-service 앱 컨테이너(user-service)의 Spring Boot Actuator 메트릭 수집
  - job_name: user-service
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["user-service:8081"]
